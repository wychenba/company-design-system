# ds-product-template — 本 template 預設不需 .env(密碼存 Netlify 後台 env var,不進 repo)
#
# Access control(免費)= Netlify Edge Function 自做 HTTP Basic Auth。
# 本 template 已內建 `netlify/edge-functions/basic-auth.ts` + netlify.toml [[edge_functions]] wiring;
# Edge Functions 所有方案含 free-tier 可用,`.netlify.app` 預設網址直接生效、無需自訂網域。
#
# 設密碼(30 秒,免費):在 Netlify 後台設 env var(不在這個檔、不進 repo),不靠 dashboard 開關:
#   Netlify → Site configuration → Environment variables → 加
#     STORYBOOK_BASIC_AUTH = user:password
#   (多組帳密空格分隔 "u1:p1 u2:p2";未設 = 站台公開 pass-through)
# 下次 deploy(push main / Trigger deploy)→ edge function 自動擋,瀏覽器跳原生帳密彈窗。
#
# 注意:Netlify Dashboard 的 Password Protection 開關、與 `_headers` 裡的 Basic-Auth header,
# 兩個都是 Pro 方案專屬($20/mo),free-tier 沒有 —— 所以免費請走上面的 edge function env var,
# 不要去 dashboard 開 Password Protection 當免費方案。
#
# 進階(要更好體驗才升級,非必須):
#   - Pro Password Protection($20/mo):dashboard 開關 / 美化密碼頁 / 只擋 deploy preview 放行 production
#   - Cloudflare Access(免費 50 user 真 SSO):需自訂網域 + Cloudflare proxy
#
# → 詳見 CLAUDE.md 「🔒 Access control — 免費 HTTP Basic Auth via Edge Function」段。
#
# 目前無實質 env var 需要(STORYBOOK_BASIC_AUTH 設在 Netlify 後台,不在本檔);
# 若未來新增其他 env var,再 copy 本檔成 .env(被 .gitignore 忽略)。
